import sys
N=int(sys.stdin.readline())
word_list=[]
for _ in range(N):
    word=sys.stdin.readline().strip()
    word_len=len(word)
    word_list.append((word,word_len))

#중복 삭제
word_list=list(set(word_list))

result=sorted(word_list,key=lambda x:(x[1],x[0]))

print(result)
